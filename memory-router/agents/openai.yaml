interface:
  display_name: "Memory Router"
  short_description: "Route conversational memory into structured files by user, project, type, and time."
  default_prompt: "Use $memory-router to decide, classify, route, remember, and recall durable conversational memory."
